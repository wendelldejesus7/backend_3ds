// Módulos default (padrão) devem estar sozinhos
// Ao importarmos, dispensamos {}

export default config = function (){
return {
    online: true, 
    live: true,
    health: 100
}
}
export default config; // ; e opcional